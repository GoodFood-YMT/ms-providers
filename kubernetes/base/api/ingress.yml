apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ms-providers
spec:
  ingressClassName: nginx
  rules:
    - host: goodfood.localdev.me
      http:
        paths:
          - pathType: Prefix
            path: /providers
            backend:
              service:
                name: ms-providers
                port:
                  number: 3333
